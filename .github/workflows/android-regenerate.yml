- name: Detect Android v1 plugins
  run: |
    echo "🔍 Scanning plugins for Android v1 embedding..."
    grep -R "io.flutter.app.FlutterApplication" ~/.pub-cache/hosted || true
    grep -R "PluginRegistry.Registrar" ~/.pub-cache/hosted || true
