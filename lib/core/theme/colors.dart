import 'package:flutter/material.dart';

class RColors {
  static const bg = Color(0xFF070B16);
  static const card = Color(0xFF0E1424);
  static const neon = Color(0xFF3DA5FF);
  static const white = Colors.white;
  static const muted = Colors.white54;
}
