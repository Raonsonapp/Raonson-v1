import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFF1DB954); // Raonson Green
  static const Color secondary = Color(0xFF0FB9B1);
  static const Color accent = Color(0xFF4B7BEC);

  // Backgrounds
  static const Color background = Color(0xFFF7F9FC);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1E272E);
  static const Color textSecondary = Color(0xFF6D7A88);
  static const Color textDisabled = Color(0xFFB0BEC5);

  // Status
  static const Color success = Color(0xFF20BF6B);
  static const Color warning = Color(0xFFF7B731);
  static const Color error = Color(0xFFEB3B5A);
  static const Color info = Color(0xFF3867D6);

  // UI
  static const Color divider = Color(0xFFE5E9F0);
  static const Color border = Color(0xFFD1D8E0);
  static const Color overlay = Color(0x66000000);
}
