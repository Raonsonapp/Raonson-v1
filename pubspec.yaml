name: raonson
description: Raonson social network MVP
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ---- NETWORK ----
  http: ^1.2.0

  # ---- LOCAL STORAGE (token) ----
  shared_preferences: ^2.2.2

  # ---- UI ----
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
    video_player: ^2.8.2

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
